# Backend host/port (used as http://HOST:PORT). Restart Vite after changing.
VITE_API_HOST=localhost
VITE_API_PORT=5000
# Optional full base URL (overrides host/port when set):
# VITE_API_URL=https://api.example.com
